# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: SpicyPizza/create-envfile@v1.3
        with:
          fail_on_empty: true
          envkey_VITE_API_KEY: ${{ secret.VITE_API_KEY }}
          envkey_VITE_AUTH_DOMAIN: ${{ secret.VITE_AUTH_DOMAIN }}
          envkey_VITE_PROJECT_ID: ${{ secret.VITE_PROJECT_ID }}
          envkey_VITE_PROJECT_ID: ${{ secret.VITE_PROJECT_ID }}
          envkey_VITE_STORAGE: ${{ secret.VITE_STORAGE }}
          envkey_VITE_MESSENGER_ID: ${{ secret.VITE_MESSENGER_ID }}
          envkey_VITE_APP_ID: ${{ secret.VITE_APP_ID }}
      - run: yarn install --frozen-lockfile && yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_BOULAISSES }}'
          channelId: live
          projectId: boulaisses
